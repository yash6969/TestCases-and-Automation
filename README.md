# Selenium POM sample (minimal)

Run tests with:

```bash
mvn test
```

Notes:
- Uses Selenium 4.11.0 so Selenium Manager will manage browser drivers automatically.
- Java 11+ recommended.
- Target URL changed to https://www.jiotv.com/ and tests run in visible Chrome by default.
